generator client {
  provider = "prisma-client"
  output   = "../prisma/generated/prisma"
}

generator prismabox {
  provider                    = "prismabox"
  typeboxImportDependencyName = "elysia"
  typeboxImportVariableName   = "t"
  inputModel                  = true
  output                      = "../prisma/generated/prismabox"
}

datasource db {
  provider = "postgresql"
}

model User {
  id        String   @id @default(cuid())
  name      String
  email     String   @unique
  password  String
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  @@map("user")
} 

model Durian {
  id          String   @id @default(cuid())
  name        String
  variety     String
  description String
  imageURL    String
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt

  @@map("durian")
}